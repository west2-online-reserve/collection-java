对于异步打印的思考

1.JAVA的异步处理流程是协商式打断，这种打断机制之前没接触过，跟RTOS的事件驱动的打断机制不同，不要考虑一些打断前的数据保存主动挂起等操作。
2.JAVA的多线程的通知式基于资源的通知，不是线程和线程之间的专向通知。相当于写了一个基于线程的竞赛规则然后让这些线程自己去竞争。
3.JAVA的多线程的方法有Thread，基于接口的Runnable和带有返回值的Callable接口。 后面得去看看
